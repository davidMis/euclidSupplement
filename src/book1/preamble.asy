size(200, 200);

// Boundaries
pen closed_boundary, open_boundary, interior, point;
closed_boundary = black + linewidth(1);
open_boundary = black + linewidth(1) + linetype("4 4");
interior = RGB(180, 180, 180);
point = black + linewidth(1);

// dot((-5, 5), white);
// dot((5, -5), white);